packages:
  - "apps/web"
  # Match packages/<group>/<name>/package.json — `@alloy/shared` lives at
  # packages/shared/ts/. Sibling python package has no package.json so pnpm
  # safely ignores it.
  - "packages/*/*"
